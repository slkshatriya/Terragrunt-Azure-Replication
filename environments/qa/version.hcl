# 
# QA Module Version
# QA uses versioned modules pinned to a specific Git tag.
# Update this to promote a new version from Dev to QA.
# 

locals {
  # Must be a valid Git tag (e.g., "v1.0.0", "v1.1.0", "v2.0.0")
  module_version = "v1.0.0"
}
